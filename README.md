# FuturFeed

This repository contains the FF design MVP exported from Figma and adapted into a Vite project.

## Running the code

1. Run `npm install` to install dependencies.
2. Run `npm run dev` to start the development server.

## Notes

- The project uses Supabase for backend services.
- Keep `.env` secrets out of GitHub and manage them in deployment settings.
